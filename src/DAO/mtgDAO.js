//const Promise = require('bluebird');
//import sqlite3 from 'sqlite3'
export default {
    /*constructor(dbFilePath) {
        this.db = new sqlite3.Database(dbFilePath, (err) => {
            if(err)
                console.err("Could not connect to database! :O")
            else
                console.log("Connected to database");
        });
    }*/
}